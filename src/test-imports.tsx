// Test file to check TypeScript module resolution
import Signup from './pages/Signup';
import Profile from './pages/Profile';

// This is just a test file to check if TypeScript can resolve the imports
console.log(Signup, Profile);
